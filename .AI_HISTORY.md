# AI History Log
This file tracks important changes and decisions in this directory and its sub-directories (when sensible).

## 2026-09-23
### Coverage catalog path
- Moved the catalog from `coverage-catalog/` to
  `test/metadata/coverage-catalog/` for the planned
  PyTest suite layout.

## 2026-09-22
### Coverage catalog for Virt Chaos dashboard
- Added `test/metadata/coverage-catalog/catalog.py`
  keyed by ci-operator `as:` names. Topic is
  Migration, Snapshot, Standard, or Upgrade.
  `catalog.json` is a generated export.
- Join is exact `as:` or Prow suffix `-{as:}`;
  longest alias wins. Unknown tests stay unclassified.
- Validator and unittest live beside the JSON.

## 2026-06-10
### Dockerfile.cli-plus-tools — fix double-install caused by GitHub suggestion apply
- Rewrote file to use `_fURL=(wget -qO) || _fURL=(curl -fsSLo)` pattern throughout.
- Removed old hardcoded `wget` blocks for yq, virtctl, uv that remained after GitHub's "Signoff and apply" only appended etirta's suggestions without deleting the originals.
- Fixed `UV_NO_MODIFY_PATH=1` → `NO_MODIFY_PATH=1` (matching EnsureReqs.sh convention).
- Removed stray `\` continuations inside `if/else` that are not needed in a heredoc.

## 2026-04-16
### Initial AI Agent Setup
- Added `.AI_INIT.md`, `.AI_README.md`, `.AI_HISTORY.md` per team Best Practices.
- Created symbolic links `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `.continuerules`, `.cursorrules` → `.AI_INIT.md`.
- Added `CONTRIBUTING.md` and `.coderabbit.yaml` per corporate mandate.
- Fixed `yq eval-all` expression in `README.md` OWNERS_ALIASES script (`ireduce` form replaces broken `select(fileIndex)` form).
